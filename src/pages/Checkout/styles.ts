import styled from 'styled-components'
import { WrapperColumn } from './components/Resume/styles'

export const CheckoutContainer = styled.main`
  margin-top: 2.5rem;
  display: flex;
  margin: 0;
  gap: 2rem;
`

export const FormContainer = styled(WrapperColumn)`
  width: 40rem;
`

export const Title = styled.h2`
  font-family: 'Baloo 2';
  font-style: normal;
  font-weight: 700;
  font-size: 1.125rem;
  line-height: 1.3;
`
